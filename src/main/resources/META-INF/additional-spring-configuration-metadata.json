{
  "properties": [
    {
      "name": "spring.ai.test.vcr.enabled",
      "type": "java.lang.Boolean",
      "description": "Whether to attach the deterministic VCR advisor to every ChatClient.Builder in the context. Off by default: a library that silently starts caching model responses is a library that silently makes a production build pass for the wrong reason.",
      "defaultValue": false
    },
    {
      "name": "spring.ai.test.vcr.mode",
      "type": "io.github.rifatcakir.springai.vcr.VcrMode",
      "description": "Record-and-replay strategy. RECORD_OR_REPLAY replays a fixture if one exists and records on a miss (the default); REPLAY_ONLY replays only, throwing VcrCacheMissException on a miss, intended for CI; RECORD_ALWAYS ignores existing fixtures and always calls the real model, overwriting them; BYPASS performs no reads or writes and always calls the real model.",
      "defaultValue": "RECORD_OR_REPLAY"
    },
    {
      "name": "spring.ai.test.vcr.scope",
      "type": "io.github.rifatcakir.springai.vcr.VcrScope",
      "description": "Where the advisor sits relative to Spring AI's auto-registered ToolCallingAdvisor. OUTSIDE_TOOL_LOOP wraps the whole tool-calling round-trip in one fixture (fastest, but @Tool methods never run on replay). INSIDE_TOOL_LOOP records one fixture per model turn so real @Tool methods keep executing on every iteration, preserving side-effect assertions.",
      "defaultValue": "OUTSIDE_TOOL_LOOP"
    },
    {
      "name": "spring.ai.test.vcr.cache-directory",
      "type": "java.lang.String",
      "description": "Directory holding the JSON fixtures, relative to the module root unless absolute. Fixtures are meant to be committed to version control alongside the tests that produced them.",
      "defaultValue": "src/test/resources/llm-cache"
    },
    {
      "name": "spring.ai.test.vcr.order",
      "type": "java.lang.Integer",
      "description": "Explicit advisor order, overriding the value scope would otherwise derive. Leave unset unless the advisor needs to interleave with other custom advisors in a specific position."
    }
  ]
}
